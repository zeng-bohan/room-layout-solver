# 更新日志

本项目的所有显著变更记录在此文件。
格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-13

### Added

- 两阶段（贴墙优先）回退式布局求解器：候选生成 + 回溯 DFS + 剩余面积剪枝。
- 冰箱开门净空三档梯子（整门扇 1220 / 半门扇 610 / 仅禁贴触 2mm），如实报告实际档位。
- 内开门 N×N 门扇摆动区与门沿禁入薄条。
- 斜墙支持：从轮廓边方向聚类允许角度集（0.5° 容差 + 精确吸附，mod 360 处理开门边朝向）。
- 零依赖 SVG 俯视图渲染（轮廓 / 门 / 门扇区 / 物品朝向标注）。
- 内置校验器：每次求解后重新推导全部约束，逐条 PASS/FAIL。
- 独立交叉校验脚本 `scripts/independent_check.py`（与主实现零共享代码）。
- 冒烟测试 `tests.py`：可行 / 不可行 / 门区阻塞 / 净空回退四类用例。
- ruff（lint + format）配置与 CI（Python 3.8 / 3.12 矩阵）。
- 可选 `pip install -e .` 安装与 `room-layout-solver` 命令行入口（pyproject.toml）。

### Fixed

- **求解确定性**：搜索预算原含 20s 墙钟截断，慢机器上会在中途放弃第一阶段搜索、
  错过全净空解（example2 实测同输入出现 610 / 1220 两种输出）。改为纯节点数预算
  （150k）后同输入输出逐字节一致（同机 4 次运行验证）。

### Changed

- 候选先过可行性过滤、后评分排序：稳定排序保证试放顺序不变（结果逐字节不变），
  example1 求解 68s → 14s（约 5×）。
